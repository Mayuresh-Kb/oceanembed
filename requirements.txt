# Core scientific stack for inspection and preprocessing.
xarray>=2024.0
netCDF4>=1.6
numpy>=1.26
scipy>=1.11
pandas>=2.2
pyyaml>=6.0
pytest>=8.0
copernicusmarine>=2.4
cdsapi>=0.7
torch>=2.5
matplotlib>=3.8
streamlit>=1.40
folium>=0.16
streamlit-folium>=0.20
plotly>=5.24

# Planned (not required for this preprocessing stage):
# xesmf
# argopy  # add during the separate ARGO-validation stage after pinning a compatible stack
# cartopy
